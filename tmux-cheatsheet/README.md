# Tmux Cheatsheet

### Download (for Linux)
sudo apt-get update
sudo apt-get upgrade
sudo apt install tmux

### Simple Tmux commands
#### Make tmux session
tmux (unamed)
tmux new -s <session-name> (named) 

